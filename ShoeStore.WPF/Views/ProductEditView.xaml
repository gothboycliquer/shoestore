<Window x:Class="ShoeStore.WPF.Views.ProductEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Height="600" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">

                <TextBlock Text="ID"
                           Margin="0,0,0,4"
                           Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <TextBox Text="{Binding Id}"
                         IsReadOnly="True"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"
                         Visibility="{Binding IsEditMode, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <TextBlock Text="Название" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Описание" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"
                         Height="80"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"/>

                <TextBlock Text="Категория" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}"
                          DisplayMemberPath="Name"
                          Padding="10,8"
                          FontSize="14"
                          Margin="0,0,0,12"/>

                <TextBlock Text="Производитель" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding Manufacturers}"
                          SelectedItem="{Binding SelectedManufacturer}"
                          DisplayMemberPath="Name"
                          Padding="10,8"
                          FontSize="14"
                          Margin="0,0,0,12"/>

                <TextBlock Text="Поставщик" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding Suppliers}"
                          SelectedItem="{Binding SelectedSupplier}"
                          DisplayMemberPath="Name"
                          Padding="10,8"
                          FontSize="14"
                          Margin="0,0,0,12"/>

                <TextBlock Text="Цена (₽)" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Единица измерения" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Количество" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Скидка (%)" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Discount, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ModernTextBoxStyle}"
                         Margin="0,0,0,12"/>

                <TextBlock Text="Изображение" Margin="0,0,0,4"/>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding ImagePath}"
                             IsReadOnly="True"
                             Style="{StaticResource ModernTextBoxStyle}"
                             Margin="0,0,8,0"/>
                    <Button Grid.Column="1"
                            Content="Выбрать"
                            Command="{Binding SelectImageCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"/>
                </Grid>

                <Image Source="{Binding ImagePath, Converter={StaticResource NullToPlaceholderConverter}}"
                       Height="150"
                       Stretch="Uniform"
                       Margin="0,0,0,12"/>

            </StackPanel>
        </ScrollViewer>

        <Border Grid.Row="1"
                Background="White"
                Padding="24,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Сохранить"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Margin="0,0,8,0"/>

                <Button Grid.Column="1"
                        Content="Отмена"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Background="#95A5A6"/>
            </Grid>
        </Border>
    </Grid>
</Window>